### Components and Props

- `CalendarMonth({ startDate, endDate, points[] })`
- `TxList({ date, items[] })`
- `TxForm({ onSubmit(tx) })`
- `CategoryEditor({ items[], onSave })`
- `A11yPanel({ fontScale, highContrast, narration, onChange })`
